apiVersion: v1
kind: Service
metadata:
  name: env-service
  labels:
    app: env-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: env-service
