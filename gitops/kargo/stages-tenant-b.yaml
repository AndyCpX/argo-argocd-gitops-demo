apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: tenant-b-dev
  namespace: env-service
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: env-service-warehouse-dev
      sources:
        direct: true
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/AndyCpX/argo-argocd-gitops-demo.git
            checkout:
              - branch: main
                path: ./repo
        - uses: kustomize-set-image
          config:
            path: ./repo/gitops/tenants/tenant-b/dev
            images:
              - image: docker.io/andycpx/kargodemo
        - uses: git-commit
          config:
            path: ./repo
            messageFromSteps:
              - kustomize-set-image
        - uses: git-push
          config:
            path: ./repo
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: tenant-b-staging
  namespace: env-service
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: env-service-warehouse
      sources:
        stages:
          - tenant-b-dev
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/AndyCpX/argo-argocd-gitops-demo.git
            checkout:
              - branch: main
                path: ./repo
        - uses: kustomize-set-image
          config:
            path: ./repo/gitops/tenants/tenant-b/staging
            images:
              - image: docker.io/andycpx/kargodemo
        - uses: git-commit
          config:
            path: ./repo
            messageFromSteps:
              - kustomize-set-image
        - uses: git-push
          config:
            path: ./repo
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: tenant-b-prod
  namespace: env-service
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: env-service-warehouse
      sources:
        stages:
          - tenant-b-staging
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/AndyCpX/argo-argocd-gitops-demo.git
            checkout:
              - branch: main
                path: ./repo
        - uses: kustomize-set-image
          config:
            path: ./repo/gitops/tenants/tenant-b/prod
            images:
              - image: docker.io/andycpx/kargodemo
        - uses: git-commit
          config:
            path: ./repo
            messageFromSteps:
              - kustomize-set-image
        - uses: git-push
          config:
            path: ./repo
